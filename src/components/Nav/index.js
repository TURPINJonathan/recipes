import React from 'react';

import './nav.scss';

const Nav = () => (
  <nav className="nav">
    <a className="nav-item nav-item--active">Accueil</a>
    <a className="nav-item">Recette 1</a>
    <a className="nav-item">Recette 2</a>
  </nav>
);

export default Nav;
