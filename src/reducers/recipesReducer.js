const initialState = {
  recipesList: [],
};

function recipesReducer(state = initialState, action = {}) {
  switch (action.type) {
    default:
      return state;
  }
}

export default recipesReducer;
